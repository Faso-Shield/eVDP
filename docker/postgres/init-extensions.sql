-- Extensions PostgreSQL utilisees par eVDP.
-- pg_trgm accelere la recherche textuelle sur les titres et identifiants.
CREATE EXTENSION IF NOT EXISTS pg_trgm;
CREATE EXTENSION IF NOT EXISTS unaccent;
